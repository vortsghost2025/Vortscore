from setuptools import setup, find_packages

setup(
    name="vitalis_core",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        "numpy",
        "huggingface_hub",
        "pyyaml",
        "click"
    ],
    entry_points={
        "console_scripts": [
            "vitalis=vitalis.__main__:cli"
        ]
    },
)
